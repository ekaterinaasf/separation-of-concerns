// what's going on here?
// replace the __ with a type to pass the asserts

console.assert(
  (typeof (typeof false)) === __,
  'first challenge'
);

console.assert(
  (typeof (typeof null)) === __,
  'first challenge'
);

console.assert(
  (typeof (typeof 0)) === __,
  'first challenge'
);

console.assert(
  (typeof (typeof undefined)) === __,
  'first challenge'
);

console.assert(
  (typeof (typeof "")) === __,
  'first challenge'
);

