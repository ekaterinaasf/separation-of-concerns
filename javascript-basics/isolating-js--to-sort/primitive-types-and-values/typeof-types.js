// all types names are "string"s.

console.assert(
  (typeof "string") === 'string',
  '"string" is type "string"'
);

console.assert(
  (typeof "number") === 'string',
  '"number" is type "string"'
);

console.assert(
  (typeof "boolean") === 'string',
  '"boolean" is type "string"'
);

console.assert(
  (typeof "undefined") === 'string',
  '"undefined" is type "string"'
);

console.assert(
  (typeof "object") === 'string',
  '"object" is type "string"'
);
