// write code to pass asserts a, b
// fill in the blanks for c and temp

let a = 2, b = 1, temp = null;

temp = a;
const c = temp;
// write your code below here



console.assert(a === 1, "asserting a");
console.assert(b === 2, "asserting b");
console.assert(c === _, "asserting c");
console.assert(temp === _, "asserting temp");
