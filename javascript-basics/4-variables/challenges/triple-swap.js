let a = 3;
let b = 1;
let c = 2;
let temp = null;

// write some code!



console.assert(a === 1, "a should store 1, it has: " + a);
console.assert(b === 2, "b should store 2, it has: " + b);
console.assert(c === 3, "c should store 3, it has: " + c);
console.assert(temp === _, "what is temp's final value?");
